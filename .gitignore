# Binaries
bin/
/muxterm
/bridge-poc
*.exe
*.dll
*.so

# Frontend
node_modules/
web/dist/
# Standalone home-view preview build (vite.demo.config.ts)
web/dist-demo/
# Mobile-navigation mockup build (vite.mobile-demo.config.ts)
web/dist-mobile-demo/
# web/package-lock.json — committed for reproducible CI builds

# IDE/Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Build/Test
.superpowers/
.superpowers-checkpoint.json
.pytest_cache/
__pycache__/

# OS
.DS_Store
Thumbs.db

# Playwright CLI session artifacts
.playwright-cli/

# Scratch / reference material (not source)
tmp/

# Digital Twin Universe generated profiles (ephemeral, workspace-specific)
.amplifier/digital-twin-universe/

# Git worktrees
.worktrees/

# goal-batch runtime artifacts
DONE.json
.amplifier/goals/manifest.tsv
.amplifier/goals/manifest-*.tsv
.amplifier/goals/*.log

# Local scratch space (font bake-off, experiments)
.scratch/

# Playwright CLI workspace config + session artifacts
.playwright/
