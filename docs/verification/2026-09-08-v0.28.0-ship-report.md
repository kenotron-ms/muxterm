# v0.28.0 ship report — finish #106: merge, verify the bypass, release, deploy

**Date:** 2026-09-08 · **Lane:** `pr 106` (workspace w9) ·
**Worktree:** `/home/ken/workspace/muxterm-finish-106` · **Branch:** `lane/finish-106`

Written and pushed **before** the deployment restart, deliberately: the restart
terminates every process in `muxterm-sessiond.service`'s cgroup, including the
pane this lane is running in. Section F6 is appended by a watchdog running in
its own transient `systemd-run --user` unit, outside that cgroup, so the
post-restart evidence does not depend on this session surviving.

---

## Verdict summary

| Item | Verdict |
|---|---|
| F1 — ground truth before touching anything | **ANSWERED** |
| F2 — what happened to #106 | **ANSWERED** — deliberately excluded from v0.27.0, no blocker recorded |
| F3 — merge #106 | **MERGED** — `ac09ed6`, 2026-09-08T23:47:36Z |
| F4 — the three safety properties on merged main | **PASS** — all three, verified at runtime |
| F5 — cut the release | **PUBLISHED** — `v0.28.0`, 2026-09-08T23:50:59Z |
| F6 — publishing works end to end on the live instance | see F6 (appended post-restart) |

---

## F1 — ground truth

**Running binary was v0.27.0, matching the newest tag.** No `(deleted)` marker on
either serving process, and the running image is byte-identical to the on-disk one:

```
lrwxrwxrwx 1 ken ken 0 Sep  8 23:15 /proc/1668846/exe -> /home/ken/.local/bin/muxterm   (serve --addr 127.0.0.1:9090)
lrwxrwxrwx 1 ken ken 0 Sep  8 23:15 /proc/1668845/exe -> /home/ken/.local/bin/muxterm   (sessiond)

e2b95a4af9fcd3e8e0168457b65bbf7dffdfdc5ae7bafabfd720a49a5554039d  /home/ken/.local/bin/muxterm
e2b95a4af9fcd3e8e0168457b65bbf7dffdfdc5ae7bafabfd720a49a5554039d  /proc/1668846/exe

$ muxterm version
muxterm 0.27.0 (MCP: stdio)
```

**The sessiond socket was healthy on all four checks** — a listening port alone is
not evidence, because the stranding failure #95 fixed looks healthy from outside:

```
(1) file on disk:  srw------- 1 ken ken 0 Sep  8 23:15 /run/user/1000/muxterm/sessiond.sock
(2) /proc/net/unix LISTENING entry (Flags=00010000 SO_ACCEPTCON, St=01):
    0000000049e996c8: 00000002 00000000 00010000 0001 01 27663726 /run/user/1000/muxterm/sessiond.sock
(3) the path in that entry still exists — stat: 209835 /run/user/1000/muxterm/sessiond.sock socket
(4) FRESH CLIENT DIAL: DIAL OK: connected to /run/user/1000/muxterm/sessiond.sock
```

**v0.27.0 contained the goal-lands-in-chat work (#108) and #107**, confirmed by
ancestry rather than by reading a changelog:

```
$ git merge-base --is-ancestor 1cd4faf v0.27.0   # PR #108 merge  -> YES
$ git merge-base --is-ancestor 1d108ea v0.27.0   # PR #107 merge  -> YES
```

Confirmed live rather than assumed, per the brief: `fleet_status` still shows the
finished goal lane standing, with a terminal state and its pane intact —

```
label="goalcheck marker"  workspace_id=w8  pane_id=1  mode=autonomous  state=done
done_means="the file /tmp/lgc-goalcheck/OK.txt exists and contains exactly the word shipped"
```

— which is precisely the behaviour #108 introduced. Before it, a finished goal lane
vanished.

---

## F2 — what happened to #106

The v0.27.0 lane **did** survive its restart: it appended S6 to its own report from a
watchdog outside the killed cgroup (`890b097`). #106 was neither orphaned nor blocked.
It was **deliberately excluded**, and the report says so in as many words:

```
### Deliberately not in this release

* **#106 (file publishing)** -- open, unmerged, left out on purpose: it awaits
  the user's own review because it serves content to anonymous callers.
```

No blocker was recorded against #106 anywhere. The single named gate was the user's
own review of an anonymous-serving route. **That gate is now satisfied**: the user has
explicitly asked for #106 to be merged and shipped, and F4 below is the review it was
asking for — performed against the merged tree, not against the branch in isolation.

---

## F3 — the merge

`feat/publish-file-live` branched from `f17d061` (the v0.26.0 ship report). `main` had
moved twice since, gaining #107 and #108. Rebased onto current `origin/main`:

```
$ git rebase origin/main
Rebasing (1/5) ... (5/5)
Successfully rebased and updated refs/heads/pr106-rebase.
```

**Zero conflicts.** The predicted collision in `internal/mcp` did not materialise:
#106 adds a new file `internal/mcp/tools_publish.go`; the goal-chat work touched
`internal/mcp/tools_lane.go`. The one shared point is the registration list in
`internal/mcp/run.go`, and #106 only adds a line there:

```
$ git diff origin/main -- internal/mcp/run.go
+	registerPublishTools(srv)
```

Both tool families survive, as required — the lane tools (`spawn_lane`, `fleet_status`,
`lane_transcript`, `session_send`, `run.go:698-813`) and the publishing tools
(`tools_publish.go:175-218`). Neither replaces the other.

### The one failing check, fixed minimally

The rebase surfaced two real failures — the tools/list inventory guards:

```
--- FAIL: TestMCPToolsListReturns24Tools: tools/list returned 27 tools, want 24
--- FAIL: TestMCPToolsListInsidePaneWithholdsCloseTools: returned 25 tools, want 22
```

These were **already failing on #106's own branch**, not caused by the rebase: the
branch touches no test file, and the repo's only PR check is `pr-review.yml`, an AI
reviewer — there is no test workflow on pull requests, so `go test` never ran on #106.

Fixed in `629231b` by updating both guards to count the publishing trio in
registration order. The tests are the guard working, not the guard being wrong: a new
tool on the manager surface, and on the lane surface inside a pane, is exactly the
change they exist to make someone state out loud. Stated: publishing is not
destructive against the workspace a lane's own report sits in, so the in-pane surface
keeps all three. No production code was touched to make a test pass.

Full suite green afterwards (`go test ./...` exit 0, all 8 packages `ok`), `go build`
exit 0, `gofmt -l` clean. The automated reviewer, re-run on the rebased head, closed
with **"No blockers. Ready to merge."**

```
$ gh pr view 106 --json state,mergedAt,mergeCommit
state=MERGED mergedAt=2026-09-08T23:47:36Z mergeCommit=ac09ed6e1ad032efe35e3382b7b34f16802620ee
```

---

## F4 — the three safety properties, on the merged tree

The evidence below was captured on `629231b`, the rebased branch head. The merge was a
merge commit, so the shipped tree could in principle differ from the verified one. It
does not:

```
merged main tree: f434214bc071a9dccd17a9a2fc3a30d283b1cd16
verified branch : f434214bc071a9dccd17a9a2fc3a30d283b1cd16
IDENTICAL
```

Two independent lines of evidence: a mechanical diff of route protection, and a runtime
harness driving the real `Server` in **production posture** — `BehindReverseProxy: true`,
which disables the loopback auth bypass exactly as the live instance's
`behind_reverse_proxy = true` does.

### Property 1 — the bypass is scoped to the publish prefix ONLY · PASS

Mechanically, over the whole route table:

```
main:   22 protected, 7 anonymous
merged: 26 protected, 9 anonymous

--- routes PROTECTED on main and NOT protected on merged (must be empty) ---
--- (end) ---

--- NEW anonymous routes on merged vs main ---
s.mux.HandleFunc("GET /p/_asset/doc.js", s.handlePublicAsset)
s.mux.HandleFunc("GET /p/{id}", s.handlePublicDocument)
```

Nothing lost protection. The four new owner routes are all wrapped in `protect()`
(`internal/server/server.go:260-263`); the static catch-all is still wrapped
(`server.go:286`); and #106 does not touch the auth middleware at all — its
`internal/server` diff is confined to `publish.go`, `publish_api.go`, `publish_css.go`
and the registrations in `server.go`.

The bypass is by REGISTRATION, not by a conditional — there is no flag, header or
request-derived condition that can promote a protected pattern into an unprotected one
(`server.go:207-222`). Both public patterns are fixed-shape, so no caller-controlled
component reaches the filesystem.

At runtime, every route requested with no `Authorization` header, with `StaticFS`
deliberately holding the string `THE APP BUNDLE` so a leak would show as a 200:

```
  REFUSED  /                                -> 503
  REFUSED  /index.html                      -> 503
  REFUSED  /ws                              -> 503
  REFUSED  /api/config                      -> 503
  REFUSED  /api/files                       -> 503
  REFUSED  /api/prs                         -> 503
  REFUSED  /api/tunnels                     -> 503
  REFUSED  /api/publications                -> 503
  REFUSED  /api/remotes                     -> 503
  REFUSED  /api/update/status               -> 503
  REFUSED  /api/ai/status                   -> 503
  REFUSED  /api/cos/voice/trace             -> 503
  REFUSED  /t/abc/                          -> 503
  SERVED   /api/health                      -> 200  {"status":"ok"}
  REFUSED  /p/AAAAAAAAAAAAAAAAAAAAAA        -> 404
  REFUSED  /p/_asset/doc.js                 -> 404
```

503 is the middleware failing closed; the handler never runs. On the same server
config a real `/p/{id}` returns 200 with content (P2, read 1) — so the contrast is
exact: the publish route serves anonymously, every other route does not.

### Property 2 — pinned by inode, re-validated on EVERY read · PASS

The pin is device+inode, not name (`publish.go:206-213`), captured at publish
(`publish.go:294-309`). Every read goes `handlePublicDocument` → `readPinned`
(`publish_api.go:134`) → `openPinned` (`publish.go:605`), whose own comment reads
"Called on EVERY read, never once at publish." `openPinned` compares dev/ino against
the pin and refuses (`publish.go:615`). `openPinnedPath` (`publish.go:574`) opens with
`syscall.O_NOFOLLOW`, and before that `ensureNoSymlinkComponents` (`publish.go:541`)
walks **every** component with `os.Lstat` and refuses any symlink — closing the
directory-swap attack `O_NOFOLLOW` alone does not cover. The caller receives an open
fd, so the check-to-read window is closed.

Three attacks, run against the real handler:

```
  published .../note.txt  pinned dev=224 ino=763596
  read 1 (untouched)                    -> 200  "ORIGINAL CONTENT"
  read 2 (file -> symlink to secret)    -> 409  "This link is broken: the file it points to is not the file that was published, so nothing was served."
  read 3 (same name, NEW inode)         -> 409  "This link is broken: ..."
  read 4 (dir case, untouched)          -> 200  "DIR-ORIGINAL"
  read 5 (PARENT DIR -> symlink)        -> 409  "This link is broken: ..."
```

The secret's bytes never appeared in any response. Replacing the file, or a directory
component of its path, with a symlink produces a REFUSAL, not the new target.

### Property 3 — every publication carries a TTL; revocation is immediate · PASS

```
  ttl omitted        -> expires_at=2026-09-09T23:38:57Z (default 24h0m0s, not forever)
  ttl 720h (>cap 168h0m0s) -> refused: ttl 720h0m0s exceeds the maximum of 168h0m0s for a public link
  before expiry      -> 200  "TTL-CONTENT"
  after expiry       -> 410  "This link has expired. It stopped working at 2026-09-08T23:38:59Z."
  before revoke      -> 200
  Revoke()=true, NEXT request -> 404  "This link is not valid."
  Cache-Control on a live publication: "no-store, no-cache, must-revalidate, private"
```

No code path creates a publication without an expiry: `Create` substitutes the 24h
default for 0 and rejects anything over the 7-day cap rather than clamping
(`publish.go:271-280`). Revocation is immediate because nothing is copied at publish
and every public response is `no-store` (`publish_api.go:224-228`). The registry is
in-memory by design, so a restart revokes everything — the fail-closed direction
(`publish.go:243-247`).

The harness was a throwaway file, used to produce the output above and then deleted.
It is not part of the merge.

---

## F5 — the release

```
tag=v0.28.0 draft=false prerelease=false published=2026-09-08T23:50:59Z
checksums.txt
muxterm_darwin_amd64.tar.gz
muxterm_darwin_arm64.tar.gz
muxterm_linux_amd64.tar.gz
```

Cut the way this repo cuts releases: an annotated tag pushed to `origin`, which fires
`.github/workflows/release.yml` → GoReleaser. Nothing hand-rolled.

**What v0.28.0 contains:** one feature merge, #106, and the test-guard update that came
with it. Everything else in the range is the v0.27.0 ship report, which is docs.

```
$ git log --oneline v0.27.0..v0.28.0
ac09ed6 Merge pull request #106 from kenotron-ms/feat/publish-file-live
629231b test(mcp): the tool inventory guards count the publishing trio
2ed1c53 docs: README names the publishing tools and the exposure they create
f0a9a86 feat(files): publish a file to a public URL from the Files applet
b0a206b verify(publish): browser proof of the live semantics and their guard
cb2fb31 feat(mcp): publish_file, list_publications, revoke_publication
41582b5 feat(publish): live public file publishing -- registry, pinned reads, /p/{id}
890b097 docs: v0.27.0 ship report -- S6, the post-restart confirmation
fa1401f docs: v0.27.0 ship report -- wait, merge, release, deploy, confirm
```

### Deployment plan, stated before the restart

**Currently serving:** `muxterm 0.27.0 (MCP: stdio)`, pids 1668845 (sessiond) and
1668846 (serve), both `/proc/<pid>/exe -> /home/ken/.local/bin/muxterm`, no `(deleted)`
marker. The server agrees an upgrade is available and that it can perform it itself:

```
{"currentVersion":"0.27.0","latestVersion":"0.28.0","updateAvailable":true,"canUpdate":true,"devBuild":false,"method":"binary"}
```

**Mechanism:** `POST /api/update/apply` — muxterm's own upgrade machinery
(`internal/update.Apply` + `internal/update.Restart`), which downloads the release
asset, verifies it against `checksums.txt`, renames the new binary over
`~/.local/bin/muxterm`, and then restarts **both** units in one `systemctl --user
restart muxterm-sessiond.service muxterm.service` invocation so ordering is
guaranteed. No hand-rolled stop and start.

**Rollback artifact — confirmed to exist, twice over:**

1. Local, no network required. The current v0.27.0 binary was copied aside before the
   upgrade, byte-identical to what is running:
   ```
   e2b95a4af9fcd3e8e0168457b65bbf7dffdfdc5ae7bafabfd720a49a5554039d  /home/ken/tmp/finish-106-rollback/muxterm-v0.27.0
   e2b95a4af9fcd3e8e0168457b65bbf7dffdfdc5ae7bafabfd720a49a5554039d  /home/ken/.local/bin/muxterm
   $ /home/ken/tmp/finish-106-rollback/muxterm-v0.27.0 version
   muxterm 0.27.0 (MCP: stdio)
   ```
2. The published v0.27.0 GitHub release still carries `muxterm_linux_amd64.tar.gz` and
   `checksums.txt`.

`internal/update.Apply` keeps no backup of its own — it renames the new binary over the
old one (`apply.go:105`) — which is exactly why (1) was taken first.

**Exact command that restores the previous version:**

```bash
install -m 0755 /home/ken/tmp/finish-106-rollback/muxterm-v0.27.0 /home/ken/.local/bin/muxterm && \
  systemctl --user restart muxterm-sessiond.service muxterm.service
```

If that copy were ever lost, the network equivalent:

```bash
cd "$(mktemp -d)" && gh release download v0.27.0 -R kenotron-ms/muxterm -p 'muxterm_linux_amd64.tar.gz' -p 'checksums.txt' && \
  sha256sum -c --ignore-missing checksums.txt && tar xzf muxterm_linux_amd64.tar.gz muxterm && \
  install -m 0755 muxterm /home/ken/.local/bin/muxterm && \
  systemctl --user restart muxterm-sessiond.service muxterm.service
```

### What is running immediately before the restart, and what is at risk

Checked, not assumed.

**At risk — killed by the restart:**

* This lane's own pane and session (`w9` p1). Mitigated: this report is written and
  pushed first, and F6 runs from a watchdog outside the cgroup.
* The finished goal lane `goalcheck marker` (`w8` p1, state `done`). Its pane is
  standing only because #108 made it stand. Its **verdict survives** in
  `~/.local/share/muxterm/completions.json`, which is on disk and outlives restarts;
  what ends is the idle pane. Session restore is enabled
  (`[restore] enabled = true`), so the workspace itself comes back.

**Not at risk — separate systemd user units, outside muxterm's cgroup:**
`muxterm-proxy.service` (Caddy, pid 538, the 8311 bridge), `amplifier-resolve`,
`amplifier-app-resolve`, `amplifier-docs-site`, `amplifier-docs-oauth`,
`amplifier-wiki-site`, `amplifier-wiki-oauth`, `fileserver`, `zellij-web`,
`vela-agentd`, `vela-agent-serve`, `vela-ledger-service`, `ssh-agent`, `dbus`.
Each survived the 23:15 v0.27.0 restart in place — Caddy's pid 538 dates from Sep 5.

**Not at risk — different machine:** the `muxterm-rp` Incus container running its own
`/home/muxuser/muxterm/bin/muxterm` (pids 844392, 844457). A local restart does not
reach it.

**Not running at all:** the static servers named in the brief as the user's, on ports
8477, 8478 and 8479. Nothing is listening on any of the three. They were not stopped by
this lane and were not touched.

**No other agent lane is at risk.** `fleet_status` lists exactly two sessions: this one
and the finished `goalcheck marker`. Every branch belonging to a *running* lane is
therefore accounted for. Branches in other worktrees that sit ahead of their upstream
belong to lanes that are no longer running; their commits are on disk and a restart
does not touch git state. They were left exactly as found — pushing another lane's
half-finished branch on its behalf would be the destructive act, not the safe one.

**`~/.config/muxterm/config.toml` must not be disturbed.** Recorded before the upgrade
so the claim can be checked after:

```
0a699ed7cc9a84430543e2ba83bb0ee5f0d24a457705aa6a9c98fde72e14faaa  /home/ken/.config/muxterm/config.toml
```

with `[voice] model = "gpt-realtime-2.1-mini"`, `auth_mode = "entra"`,
`[server] public_origin = "https://muxterm.ampbox.io"`, `behind_reverse_proxy = true`.

### The before/after that F6 will close

On the running v0.27.0 instance, right now:

```
GET /api/publications (authed)          -> 404   (route does not exist yet)
GET /p/AAAAAAAAAAAAAAAAAAAAAA (no auth) -> 401   (falls through to the protected static handler)
GET /                          (no auth) -> 401
```

After the upgrade, `/p/{id}` must become reachable without auth while `/` stays 401.
That pair is the whole claim.

---

## F6 — end-to-end on the live instance

**PENDING.** Appended by the watchdog after the restart. If this still says PENDING,
the watchdog did not survive either, and the checks must be re-run by hand from the
commands listed above.
