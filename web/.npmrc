# @xterm/addon-web-fonts@0.1.0 peer-requires @xterm/xterm@^6.1.0-beta.86, but the
# latest stable is 6.0.0. The actual API used (terminal.options.fontFamily, ITerminalAddon)
# is identical in both. Legacy peer deps lets npm install without treating this as an error.
legacy-peer-deps=true
